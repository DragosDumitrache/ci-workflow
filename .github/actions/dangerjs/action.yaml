name: 'dangerjs'
inputs:
  dangerfile:
    description: Which dangerfile to use
runs:
  using: composite
  steps:

    - name: DangerJS
      run: |
        yarn danger ci --dangerfile ${{ inputs.dangerfile }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
